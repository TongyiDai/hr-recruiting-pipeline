# 招聘漏斗分析｜Agent 使用须知

本须知适用于所有能够读取 `SKILL.md`、执行本地脚本并调用飞书 CLI 的 Agent。它不依赖某一家 Agent 平台的提示词格式或界面配置。

本 Skill 用于招聘数据的只读整理和描述性分析。Agent 的任务是把授权数据变成可复核摘要，保留招聘负责人对候选人的判断权。

## 跨 Agent 使用原则

- 任何 Agent 都先遵循本文件和根目录 `SKILL.md`，平台侧的展示名称或快捷入口只负责发现与触发。
- Agent 能读取用户提供的脱敏文件时，可以跳过飞书读取，直接执行本地分析并报告输入范围。
- Agent 无法执行 `lark-cli` 或无法确认用户身份时，停止真实数据读取，说明限制，不用其他账号替代。
- Agent 可以生成分析草案和人工复核清单，不能把平台能力差异解释成授权扩大。

## 什么时候使用

用户提到以下内容时触发：

- 招聘进展、候选人流水、招聘状态。
- 简历筛选、面试阶段、面试后反馈、Offer 进展。
- 招聘漏斗、招聘周期、渠道效果、阶段积压。
- 用飞书 Base、Sheets 或脱敏文件分析招聘数据。

用户只是在写招聘文案、设计面试题或起草 Offer 时，使用对应的其他 Skill。

## 先确认四件事

1. 数据源：飞书 Base、Sheets、JSON、CSV 或用户直接提供的摘要。
2. 时间范围：本周、本月、指定日期区间，或数据的完整周期。
3. 统计口径：按当前阶段、阶段历史、岗位、招聘人、地点或渠道分组。
4. 输出目的：进展汇报、瓶颈定位、渠道比较、周期估算或数据质量检查。

数据源或时间范围缺失时，先采用最窄范围并明确写出假设；无法形成可复核范围时暂停分析。

## 标准执行顺序

### 1. 校验身份

```bash
lark-cli auth status --json --verify
```

只有在支持 `auth status --json --verify` 的环境里确认 `identity=user`、`verified=true` 且授权有效后，或当前 CLI 构建没有 `auth` 子命令但 `contact +get-user --as user` / `task +get-my-tasks --as user` 的只读兼容探测通过后，才能读取用户飞书数据。账号、租户或权限存在疑问时停止。

### 2. 读取最小字段

优先读取：`candidate_id`、`stage`、`entered_at`、`updated_at`、`source`、`job`、`recruiter`、`stage_history`、`accepted_at`。字段映射参考 [input-schema.md](references/input-schema.md)。

先解析资源和字段，再分页读取记录。数据量较大时缩小时间范围或字段范围。

### 3. 本地聚合

将飞书结果保存为脱敏 JSON/CSV，在本地运行：

```bash
python3 scripts/analyze_pipeline.py \
  --input tests/fixtures/pipeline.json \
  --format markdown \
  --as-of 2026-08-12
```

脚本不访问网络，不写回飞书，不在报告中输出候选人编号原值。

### 4. 组织结果

按以下顺序输出：

1. 结论：当前漏斗变化或最明显的瓶颈。
2. 范围：数据源类型、时间窗、过滤条件和记录数。
3. 指标：阶段人数、转换率、阶段时长、招聘周期和来源效果。
4. 质量：缺失字段、未知阶段、日期异常、重复编号和历史完整度。
5. 边界：哪些内容是数据事实，哪些内容需要招聘负责人解释。
6. 动作草案：可供人工确认的下一步，不自动执行写入。

## 输出要求

- 聚合数据优先，候选人级明细默认不输出。
- 缺失或无法核验的内容标记为 `NA`、缺失或待确认。
- 样本量很小的渠道只做提示，不写成稳定结论。
- 区分数据事实、规则计算和人工判断。
- 结果中保留数据范围、字段映射和证据来源。

## 必须停止的情况

- 用户身份、租户或授权范围无法确认。
- 目标数据包含大量姓名、联系方式、简历正文或面试逐字稿，且没有必要的脱敏范围。
- 阶段、日期或记录关联无法形成稳定口径。
- 用户要求自动淘汰、录用、排序候选人，或直接作出薪酬、晋升判断。
- 用户要求改写飞书数据、发消息、发 Offer、建日程或修改权限。

停止时说明具体阻塞点，并给出恢复所需的最小信息。

## 典型请求

```text
看一下本周产品经理招聘漏斗，按当前阶段统计，告诉我哪个阶段积压最明显。
```

```text
读取我授权的飞书招聘流水，比较内推和招聘网站的到面率与 Offer 接受率。
```

```text
用这份脱敏 CSV 估算从进入流程到接受 Offer 的中位天数，并列出数据质量问题。
```

## 不承担的工作

本 Skill 不代替 HR 做录用、淘汰、薪酬、晋升或合规决定，也不执行候选人状态修改、消息发送、面试创建和 Offer 发放。
